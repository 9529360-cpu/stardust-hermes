# Stardust Jarvis 改造计划

## 目标
把 Stardust 逐步做成阿豪的长期私人助理：本地常驻、可恢复、可记忆、可调用模型和工具，能够在明确权限内替用户完成电脑事务，而不是一次性聊天壳。

## 不变边界
- Stardust 是唯一主线；ZN 不再继续施工。
- 所有外部发送、交易、删除、公开发布和不可逆动作必须有权限边界与确认机制。
- 模型负责理解、推理和陌生问题；本地系统拥有记忆、技能、任务状态、恢复、权限和常规能力。
- 任何功能必须有真实运行证据，不能用“代码写了/CI绿了”代替产品验收。
- Claude 与 Codex 使用独立 worktree，不得共享可写目录，不得互相覆盖。

## 第一阶段验收主线
1. 真实聊天：流式输出、停止、错误恢复。
2. 模型接入：官方/兼容 API 配置、健康检查、失败提示，不泄露密钥。
3. 记忆：显式记住/忘记、重启后仍在、可查看和删除。
4. Work：长任务状态、取消、重启恢复、结果和证据。
5. 权限：危险动作前确认，低风险本地动作可执行并可审计。
6. 桌面：真正可用的简洁工作台，不只是 CSS 改色。

## 交接规则
每个代理开始前读取本文件和根目录 AGENTS.md；完成后必须写入自己的 REPORT 文件，包含分支、commit、变更文件、真实测试输出、未完成项和风险。没有这些证据不得合并。
