# Stardust Jarvis 改造计划

## 目标
把 Stardust 逐步做成阿豪的长期私人助理：本地常驻、可恢复、可记忆、可调用模型和工具，能够在明确权限内替用户完成电脑事务，而不是一次性聊天壳。

## 不变边界
- Stardust 是唯一主线；ZN 不再继续施工。
- 外部发送、交易、删除、公开发布和不可逆动作必须有明确授权边界；用户已经明确授权且对象、范围、金额和权限没有发生实质变化时，不重复确认。只有授权范围扩大、目标或金额不明确、权限边界变化，或风险显著上升时才重新确认。
- 模型负责理解、推理和陌生问题；本地系统拥有记忆、技能、任务状态、恢复、权限和常规能力。
- 任何功能必须有真实运行证据，不能用“代码写了/CI绿了”代替产品验收。
- 并行任务使用独立、短生命周期的 task branch/worktree，并从当前 main 创建；不同任务不得共享可写目录，也不得按代理长期保留固定开发副本。

## 第一阶段验收主线
1. 真实聊天：流式输出、停止、错误恢复。
2. 模型接入：官方/兼容 API 配置、健康检查、失败提示，不泄露密钥。
3. 记忆：显式记住/忘记、重启后仍在、可查看和删除。
4. Work：长任务状态、取消、重启恢复、结果和证据。
5. 权限：危险动作需要明确授权边界和审计；已有充分授权时不机械重复确认，只有范围或风险实质变化时重新确认。
6. 桌面：真正可用的简洁工作台，不只是 CSS 改色。

## 交接规则
每个代理开始前读取本文件和根目录 AGENTS.md。完成后的权威交接证据放在对应 PR / issue / commit 和 CI / 真实运行验证中，至少说明分支、commit、变更文件、验证结果、未完成项和风险。不要为每次任务强制新增永久 REPORT 文件，避免形成与 Git/PR 状态并行的第二套事实源；只有用户明确要求或事故复盘确有长期价值时才新增报告文档。
