# Codex 施工任务：模型接入与安全权限闭环

## 工作目录
`D:\项目\Stardust-Jarvis` 的独立 worktree；不要修改主目录，不要碰 ZN。与 Claude 文件范围分离。

## 目标
审计并改进 Stardust 的 Provider/模型设置及敏感操作权限，使普通用户能可靠配置模型、知道模型是否可用、并在危险动作上得到清楚确认。

## 文件范围
仅限 Provider 配置、凭据存储/读取、权限确认与对应测试。不要改聊天 Work/恢复核心，不改全局桌面布局；若发现跨界需要，先写报告请求协调。

## 必须先做
1. 只读审计根 `AGENTS.md`、Provider 配置/凭据路径、相关 UI 和测试。
2. 写 `agent-reports/codex-start.md`：现状、风险、文件范围、验收方法。

## 验收
- 用户可完成模型配置与连通性检查；错误提示可操作且不泄露密钥。
- 凭据不写日志、不出现在诊断/导出中。
- 危险外部操作遵守已有授权/确认机制。
- 补测试并提供命令和真实输出。
- 不引入 ZN 依赖，不自动合并。

## 完成报告
写 `agent-reports/codex-report.md`：分支、commit、改动文件、测试证据、实测限制与风险。
