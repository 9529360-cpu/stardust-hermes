# Claude 施工任务：Resident / Work / 恢复闭环

## 工作目录
`D:\项目\Stardust-Jarvis` 的独立 worktree；不要修改主目录，不要碰 ZN。

## 目标
把 Stardust 现有聊天、Work、记忆和重启恢复整理成一个可真实验收的长期助理闭环。重点不是继续堆抽象层，而是找出现有实现的断点并补齐最短路径。

## 必须先做
1. 只读审计根目录 `AGENTS.md`、当前聊天入口、Work 状态持久化、恢复入口和已有测试。
2. 写 `agent-reports/claude-start.md`：当前基线、问题、计划、明确不改的范围。

## 施工优先级
1. 聊天提交 → Resident/Work → 流式结果 → 持久化。
2. 进程重启后恢复活动 Work 和历史消息。
3. 明确取消、失败、等待用户确认三种状态。
4. 记忆显式 remember/forget 的真实读回验证。
5. 补关键回归测试和 Windows 实机启动验证。

## 禁止
- 不要引入 ZN 代码、路径、数据库或运行时依赖。
- 不要把模型直接变成权限主体。
- 不要用假数据或只改 UI 冒充完成。
- 不要自动合并；完成后提交分支和报告，等待审查。

## 完成报告
写 `agent-reports/claude-report.md`，包括分支、commit、完整变更清单、命令和真实输出、已知缺口。
